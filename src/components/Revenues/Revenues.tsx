import { Container, Header, Title } from './styles';

export const Revenues = ((): JSX.Element => {
  return (
    <Container>
      <Header>
        <Title>Revenues</Title>
      </Header>
    </Container>
  );
});