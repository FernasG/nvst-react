import { Container, Header, Title } from './styles';

export const Courses = ((): JSX.Element => {
  return (
    <Container>
      <Header>
        <Title>Courses</Title>
      </Header>
    </Container>
  );
});