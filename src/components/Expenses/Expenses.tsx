import { Container, Header, Title } from './styles';

export const Expenses = ((): JSX.Element => {
  return (
    <Container>
      <Header>
        <Title>Expenses</Title>
      </Header>
    </Container>
  );
});